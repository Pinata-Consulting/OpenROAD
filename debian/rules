#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- -DCMAKE_LIBRARY_ARCHITECTURE="$(DEB_TARGET_MULTIARCH)"

override_dh_shlibdeps:
	dh_shlibdeps -- --ignore-missing-info -l/opt/or-tools/lib
